# Core Django
Django==5.2.8
psycopg2-binary==2.9.9
python-decouple==3.8

# REST API (not yet wired up — planned for public APIs)
djangorestframework==3.14.0

# Code Execution
# NOTE: RestrictedPython is OPTIONAL hardening for the sandbox executor —
# the executor strengthens automatically when the package is installed.
RestrictedPython==7.0

# File Handling
Pillow==10.1.0

# PDF Generation (certificates — planned)
weasyprint==60.1
reportlab==4.0.7

# Markdown Support
markdown==3.5.1

# AI/LLM Integration
anthropic==0.75.0

# Video Generation (planned — video_generator app is not implemented yet)
manim==0.18.0
ffmpeg-python==0.2.0

# Task Queue (planned — Celery app not wired up yet)
celery==5.3.4
redis==5.0.1

# Monitoring and Logging (production uses Sentry when SENTRY_DSN is set)
sentry-sdk==1.38.0

# Testing
pytest==7.4.3
pytest-django==4.7.0
factory-boy==3.3.0

# Production Server
gunicorn==21.2.0
whitenoise==6.6.0

# Development
django-debug-toolbar==4.2.0
django-extensions==3.2.3
